# Frontend features

Organise user-facing functionality by feature rather than by technical file type. Suggested future feature directories include `auth`, `fixtures`, `submissions`, `statistics`, `datasets`, and `api-consumers`.
