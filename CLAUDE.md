# Claude Code repository guidance

Read and follow `AGENTS.md` for the repository's shared coding-agent workflow.

When present, `gitea-export/ai-project-context.md` is the local entry point for repository structure
and exported Gitea issue context. It is a snapshot rather than a live source, contains no access
credential, and must not be committed.
