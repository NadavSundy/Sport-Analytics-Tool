# Migrations

Store ordered SQL migrations here. Use a sortable timestamp prefix, for example:

```text
202608041900_create_competitions.sql
202608051030_create_fixtures.sql
```

The migration tool and exact commands must be documented once selected. Do not commit generated API endpoints or database credentials.
